var productModel = require("../../models/products")

const addProduct = (params) => {
    return new Promise((resolve, reject) => {
        var r = {
            status: true,
            msg: "Yet to handle event"
        }
        resolve(r)
    })
}

module.exports = addProduct



