var categoryModel = require("../../models/category")
var productModel = require("../../models/products")

const statusChecker = (params) => {
    return new Promise((resolve, reject) => {
        
    })
}

module.exports = statusChecker